# Upstream API Configuration
UPSTREAM_API_KEY=your-api-key-here
UPSTREAM_BASE_URL=https://api.deepseek.com

# Proxy Server Configuration
PROXY_PORT=5000
PROXY_API_KEY=              # 本地代理服务的 API Key（可选，留空则不验证）

# Default LLM Parameters (optional)
# These values are used when the client doesn't specify them.
# Client request parameters always take precedence over these defaults.

# DEFAULT_TEMPERATURE=0.7        # Controls randomness (0-2)
# DEFAULT_TOP_P=0.9              # Nucleus sampling threshold (0-1)
# DEFAULT_TOP_K=40               # Top-k sampling (1-100)
# DEFAULT_MAX_TOKENS=4096        # Maximum output tokens
# DEFAULT_PRESENCE_PENALTY=0     # Penalize new tokens based on presence (-2 to 2)
# DEFAULT_FREQUENCY_PENALTY=0    # Penalize new tokens based on frequency (-2 to 2)
# DEFAULT_SEED=                  # Random seed for reproducibility (integer)

# ============================================
# Reasoning Model Configuration (推理模型配置)
# ============================================

# 是否启用推理模式 (true/false)
# Enable reasoning mode injection
REASONING_ENABLED=false

# 推理参数类型 (用户手动选择，不自动检测)
# Reasoning parameter type (manually selected by user)
# Available options:
#   - deepseek      : DeepSeek R1/V3.1 series (thinking.type parameter)
#   - openai        : OpenAI o1/o3/GPT-5 series (reasoning_effort parameter)
#   - anthropic     : Claude 3.7/4 series (thinking.budgetTokens parameter)
#   - gemini        : Gemini 2.5+ series (thinkingConfig parameter)
#   - qwen          : Qwen3 series (enable_thinking parameter)
#   - openrouter    : OpenRouter unified format (reasoning.enabled parameter)
#   - custom        : Custom parameters (use REASONING_CUSTOM_PARAMS)
REASONING_TYPE=deepseek

# 推理强度 (部分模型类型支持)
# Reasoning effort level (supported by some model types)
# Available options: none, minimal, low, medium, high, auto
REASONING_EFFORT=auto

# 思考 Token 预算 (用于 anthropic/gemini/qwen 类型)
# Thinking token budget (for anthropic/gemini/qwen types)
# Leave empty to use default calculation based on effort level
# REASONING_BUDGET_TOKENS=16000

# 自定义推理参数 (JSON 格式)
# Custom reasoning parameters (JSON format)
# Used for 'custom' type or to override default behavior
# Example: {"thinking": {"type": "enabled"}, "include_reasoning": true}
# REASONING_CUSTOM_PARAMS={}

# 是否过滤响应中的 <think> 标签 (true/false)
# Filter <think>...</think> tags from response
FILTER_THINKING_TAGS=true
